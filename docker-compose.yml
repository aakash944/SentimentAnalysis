version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - C:/Users/ACER/Desktop/redis-data:/data
    restart: unless-stopped